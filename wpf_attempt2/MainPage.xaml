<Page x:Class="wpf_attempt2.MainPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:wpf_attempt2"
      mc:Ignorable="d" 
      d:DesignHeight="550" d:DesignWidth="800"
      Title="MainPage">

    <StackPanel Width="800" HorizontalAlignment="Center" Background="#FFFAD1D1" Orientation="Horizontal">
        <StackPanel HorizontalAlignment="Center" Orientation="Vertical" Background="#FFFAD1D1" Loaded="Grid_Loaded">
            <StackPanel Orientation="Horizontal" Width="600">
                <TextBox x:Name="textBox1" Width="150" Height="22" Margin="80,20,265,10" HorizontalAlignment="Center" VerticalAlignment="Top" KeyUp="textBox1_KeyUp" />
                <Button Width="100" VerticalAlignment="Top" HorizontalAlignment="Right" Margin="0,10,0,10" Height="40" Click="Button_Click"/>
            </StackPanel>
            <DataGrid AutoGenerateColumns="False" x:Name="dgAssets" d:ItemsSource="{d:SampleData ItemCount=15}" Margin="120,20,10,40" Width="546" Height="405">
                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Path=Rank}" IsReadOnly="True" Header="Rank" Width="180"/>
                    <DataGridTextColumn Binding="{Binding Path=Name}" IsReadOnly="True" Header="Name" Width="180"/>
                    <DataGridTextColumn Binding="{Binding Path=PriceUsd}" IsReadOnly="True" Header="Price in Usd" Width="180"/>
                </DataGrid.Columns>
                <DataGrid.Resources>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Height" Value="25"/>
                        <Setter Property="BorderThickness" Value="0"/>
                        <EventSetter Event="MouseDoubleClick" Handler="DataGridRow_MouseDoubleClick"/>
                        <Style.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Foreground" Value="Black" />
                                <Setter Property="Background" Value="LightPink" />
                            </Trigger>
                        </Style.Triggers>
                    </Style>
                </DataGrid.Resources>
            </DataGrid>
        </StackPanel>
        
    </StackPanel>
</Page>
